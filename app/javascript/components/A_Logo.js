import React from "react"
import PropTypes from "prop-types"
class ALogo extends React.Component {
  render () {
    return (
       <img src={logo} style={nbStyle.logo} alt="logo_Lir"/>
    );
  }
}

export default ALogo
